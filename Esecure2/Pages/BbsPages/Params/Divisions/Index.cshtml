@page
@*@model Bbs.Pages.Params.Divisions.IndexModel*@

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

@*<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Division[0].DivisionName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Division[0].PersonID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Division) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DivisionName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PersonID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DivisionID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.DivisionID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.DivisionID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>*@
