@page
@model Bbs.Pages.Params.RespTypes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RespType[0].RespTypeDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.RespType) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RespTypeDescription)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.RespTypeID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.RespTypeID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.RespTypeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
