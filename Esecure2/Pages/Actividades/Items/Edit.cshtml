@page
@model EsecureWebApp.Pages.Actividades.Items.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h2>Editar</h2>

<h4>ItemVerificacion</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ItemVerificacion.ItemVerificacionID" />
            <div class="form-group">
                <label asp-for="ItemVerificacion.Glosa" class="control-label"></label>
                <input asp-for="ItemVerificacion.Glosa" class="form-control" />
                <span asp-validation-for="ItemVerificacion.Glosa" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Guardar" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Volver</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
